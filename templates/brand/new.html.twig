{% extends 'base.html.twig' %}

{% block title %}Nouvelle marque{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Créer une nouvelle marque</h1>

        {{ include('brand/_form.html.twig') }}

        <a href="{{ path('app_brand_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}

